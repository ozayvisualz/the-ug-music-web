{
  "buildCommand": "prisma db push --accept-data-loss && prisma generate && npx tsx scripts/migrate-artist-names.ts && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
