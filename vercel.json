{
  "buildCommand": "npx tsx scripts/migrate-artist-names.ts && prisma db push --accept-data-loss && prisma generate && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
